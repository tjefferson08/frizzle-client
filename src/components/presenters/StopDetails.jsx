import React from 'react';

const StopDetails = ({ stop_id }) => {
  return (
    <div>
      <h3>{stop_id}</h3>
      <div>
        I'm a details card.
      </div>
    </div>
  );
};

export default StopDetails;
